## react-node-vercel (fullstack)
- Runtime/framework: react, node
- Deployment: Vercel

## features
- Using `builds` in vercel.json (cannot use `functions` and `builds` together)
- Without using `api` folder